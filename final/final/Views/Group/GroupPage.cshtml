@{
    ViewBag.Title = "GroupPage";
    string ProductID = ViewBag.Product.ObjectId;
    
}

@if (!ViewBag.NewGroupCreatedRightNow)
{
    <h2>@{ Response.Redirect("~/Group/Group");}</h2>
}
else if (ViewBag.NewGroupCreatedRightNow)
{
    <h2>קבוצת רכישה נפתחת ברגעים אלו ותיהיה זמינה עוד 12 שעות..</h2>
}












